# Gavity 会议控制台界面原型规划

## 1. 意图概述

Gavity 是一个基于罗伯特议事规则的会议流程工具，核心价值是将复杂的议事规则数字化、流程化。本原型聚焦于"单页会议控制台"，通过弹窗处理动议、投票等操作，让主持和与会者在一个界面内完成所有会议活动。

**设计约束：**
- 善用弹窗处理复杂操作（动议、投票、设置等）
- 操作受会议状态约束（除非开启记录模式）
- 与视频会议软件并存，界面简洁不抢戏

---

## 2. 用户与场景

### 2.1 用户角色

| 角色 | 核心目标 | 关键操作 |
|------|---------|---------|
| 会议主持 | 控制流程、裁决争议、推进议程 | 开启/结束会议、分配发言权、裁决动议、开启投票、开启记录模式 |
| 与会者 | 参与讨论、行使权利 | 抢发言权、提出动议、附议、投票、查看队列 |
| 观察员 | 了解会议进展 | 查看议题、发言队列、投票结果（只读） |

### 2.2 核心场景

1. **标准讨论流程**
   - 与会者抢发言权 → 进入队列 → 获得发言权 → 发言 → 结束发言
   - 发言过程中提出动议 → 附议 → 讨论 → 投票 → 通过/否决

2. **动议处理流程**
   - 提出动议（弹窗选择类型）→ 附议（达到附议数）→ 辩论（可选）→ 投票（弹窗投票）→ 结果公告

3. **记录模式**
   - 主持开启记录模式 → 解除所有操作限制 → 自由补录 → 关闭记录模式

---

## 3. 单页布局结构

### 3.1 布局概览

```
┌─────────────────────────────────────────────────────────────────┐
│  顶部栏：会议名称 | 状态徽章 | 记录模式开关 | 帮助按钮             │
├─────────────────────────────────────────────────────────────────┤
│        │                                    │                   │
│  与会  │          当前议题 / 动议           │    操作面板       │
│  者列  │          （中央舞台）              │    + 日志区域     │
│        │                                    │                   │
│        │          议程列表                  │    快捷操作       │
│        │          （可折叠）                │    实时日志流     │
│        │                                    │                   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 区域定义

| 区域 | 宽度占比 | 内容 | 交互 |
|------|---------|------|------|
| 顶部栏 | 100% | 会议名称、状态徽章（讨论中/投票中/休会）、记录模式开关、帮助按钮 | 记录模式为开关控件，帮助为弹窗 |
| 左侧栏 | 20% | 与会者列表（含角色标签、发言权状态），**无发言队列概念**，每次重新开抢 | 点击与会者查看详情（弹窗），主持可手动分配发言权 |
| 中央舞台 | 50% | 当前议题标题、议题详情、当前动议卡片（如果有）、议程列表（可折叠） | 议程可点击切换，动议卡片展开详情 |
| 右侧面板 | 30% | 操作按钮组（抢发言权、提出动议、附议、投票）、状态提示、**操作日志流（实时滚动）** | 按钮状态根据会议状态动态禁用/启用，每条操作自动推送到日志区 |

### 3.3 发言权机制（重要变更）

**无发言队列概念** — 每次抢发言权都是重新开抢：
- 点击"抢发言权"后，清除之前的抢夺记录，所有人重新抢夺
- 谁抢到由系统随机或按时间优先规则决定
- 主持可随时取消发言权或手动分配

### 3.4 动议类型体系（30+ 种）

**动议类型从下拉菜单选择，包含但不限于：**

#### 附属动议（Subsidiary Motions）
- 搁置（Lay on the Table）
- 之前的 question（Previous Question，结束辩论）
- 限时或限制辩论（Limit or Extend Limits of Debate）
- 有限期推迟（Postpone to a Certain Time）
- 委托给委员会（Refer to a Committee）
- 修改（Amend）
- 无限期推迟（Postpone Indefinitely）

#### 优先动议（Privileged Motions）
- 休会（Adjourn）
- 休息（Recess）
- 提出紧急议程（Raise a Question of Privilege）
- 调整会议秩序（Call for the Orders of the Day）
- 主席裁决定问题（Appeal from the Decision of the Chair）

#### 偶发动议（Incidental Motions）
- 程序问题（Point of Order）
- 主席裁决（Appeal）
- 暂停规则（Suspend the Rules）
- 议题拆分（Division of a Question）
- 表决方式（Method of Voting）
- 议题审议顺序（Order of Consideration）

#### 其他动议
- 主动议（Main Motion）
- 重新审议（Reconsider）
- 撤销（Rescind）
- 恢复议题（Take from the Table）

**动议约束规则：**
- 大部分动议需要发言权才能提出
- 部分特权动议和偶发动议**无需发言权**即可提出，并提出后临时夺下发言权
- 具体规则由系统根据动议类型自动判断

---

## 4. 核心组件定义

### 4.1 与会者列表组件

**内容：**
- 与会者头像 + 姓名 + 角色标签（主持/与会者/观察员）
- 发言权状态指示器（持有发言权者高亮显示）
- 在线状态

**交互：**
- 点击与会者：打开详情弹窗（统计发言次数、动议数、投票记录）
- 主持可点击"分配发言权"按钮

**内容：**
- 议题编号（如"议题 #3"）
- 议题标题
- 议题详情（可展开）
- 当前动议叠加层（如果有动议）

**状态：**
- 正常：仅显示议题
- 有动议：议题上方叠加动议卡片（带类型标签、提出人、状态）

### 4.3 动议卡片组件

**内容：**
- 动议类型徽章（程序动议/实质动议/特权动议等）
- 动议内容摘要
- 提出人、提出时间
- 附议状态（如"2/3 人已附议"）
- 操作按钮：附议、反对、开启投票（主持可见）

**状态：**
- 待附议：附议数未达标，显示附议按钮
- 已附议：附议数达标，等待辩论或投票
- 辩论中：允许发言讨论
- 投票中：显示投票按钮
- 已通过/已否决：显示结果

### 4.4 操作面板组件

**按钮组：**
1. **抢发言权**
   - 禁用条件：已有发言权、会议未开始、非讨论状态
   - 点击后：**重新开抢**，清除之前记录，最先点击者获得发言权

2. **提出动议**
   - 禁用条件：会议未开始、已有待处理动议（部分动议类型可无发言权提出）
   - 点击后：打开动议弹窗（含 30+ 种动议类型下拉菜单）

3. **附议**
   - 禁用条件：无待附议动议、已附议
   - 点击后：提交附议，更新附议计数

4. **投票**
   - 禁用条件：无进行中的投票、已投票
   - 点击后：打开投票弹窗

**操作日志区域：**
- 位于操作按钮下方，实时滚动显示所有操作记录
- 每条日志包含：操作人、操作类型、时间戳、相关内容摘要
- 示例：
  - "@张三 提出动议 #M1 修改议程顺序"
  - "@李四 附议了动议 #M1"
  - "@王五 获得发言权"
  - "#V1 投票结果：通过（5-2-1）"

### 4.5 记录模式开关

**位置：** 顶部栏右侧

**交互：**
- 默认关闭（标准模式）
- 开启时显示警告横幅："记录模式已开启，操作限制已解除"
- 主持可切换，其他角色可见但不可操作

---

## 5. 弹窗交互流程

### 5.1 动议弹窗

**触发：** 点击"提出动议"按钮

**内容：**
```
┌─────────────────────────────────────┐
│  提出动议                      [×]  │
├─────────────────────────────────────┤
│  动议类型 *                         │
│  ○ 主动议（下拉框）                  │
│                                     │
│  动议内容 *                         │
│  [________________________________] │
│                                     │
│  说明                               │
│  [________________________________] │
│                                     │
│        [取消]     [提交动议]        │
└─────────────────────────────────────┘
```

**验证：**
- 动议类型必选
- 动议内容必填
- 提交后关闭弹窗，动议卡片出现在议题上方

### 5.2 投票弹窗

**触发：** 点击"投票"按钮或投票开始时自动弹出

**内容：**
```
┌─────────────────────────────────────┐
│  投票：关于XXX的动议           [×]  │
├─────────────────────────────────────┤
│  动议摘要：                         │
│  [_______________________________]  │
│                                     │
│  您的投票 *                         │
│  ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │  赞成   │ │  反对   │ │  弃权  ││
│  └─────────┘ └─────────┘ └────────┘│
│                                     │
│  投票截止时间：XX:XX                │
│  已投票：X/Y 人                     │
│                                     │
│              [取消]     [确认投票]  │
└─────────────────────────────────────┘
```

**验证：**
- 投票选项必选
- 提交后显示结果："您的投票已提交"

### 5.3 会议设置弹窗

**触发：** 顶部栏"设置"按钮

**内容：**
- 会议基本信息（名称、议程）
- 议事规则配置（附议人数阈值、投票规则等）
- 与会者管理（邀请、角色分配）

### 5.4 与会者详情弹窗

**触发：** 点击左侧栏与会者名称

**内容：**
- 与会者基本信息
- 本次会话统计（发言次数、发言时长、提出动议数、附议数、投票记录）
- 操作按钮（主持可见）：设为主持、静音、移除

---

## 6. 状态机与操作约束

### 6.1 会议状态机

```
[未开始] → [进行中] → [休会] → [恢复] → [已结束]
              ↓
           [投票中]
```

**状态说明：**
- **未开始：** 所有操作禁用，仅主持可开启会议
- **进行中：** 允许抢发言权、提出动议（需发言权）
- **投票中：** 暂停发言权争夺，开启投票弹窗
- **休会：** 暂停所有操作，保留状态
- **已结束：** 冻结所有数据，生成会议纪要

### 6.2 操作约束矩阵

| 操作 | 未开始 | 进行中 | 投票中 | 休会 | 记录模式 |
|------|--------|--------|--------|------|----------|
| 抢发言权 | ✗ | ✓（无发言权） | ✗ | ✗ | ✓（解除所有限） |
| 提出动议 | ✗ | ✓（有发言权） | ✗ | ✗ | ✓ |
| 附议 | ✗ | ✓（有发言权、有待附议动议） | ✗ | ✗ | ✓ |
| 投票 | ✗ | ✗ | ✓（未投票） | ✗ | ✓ |
| 开启/结束会议 | ✓（仅主持） | ✓（仅主持） | ✗ | ✓（仅主持） | ✓ |

---

## 7. 数据模型

### 7.1 会议实体

```typescript
interface Meeting {
  id: string
  name: string
  status: 'not_started' | 'in_progress' | 'voting' | 'recessed' | 'ended'
  isRecordMode: boolean
  startedAt?: Date
  endedAt?: Date
  hostId: string
  agenda: AgendaItem[]
  currentAgendaIndex: number
  currentMotion?: Motion
  speakerQueue: SpeakerQueueItem[]
  participants: Participant[]
}

interface AgendaItem {
  id: string
  title: string
  description?: string
  order: number
  status: 'pending' | 'discussing' | 'voting' | 'passed' | 'rejected'
}

interface Motion {
  id: string
  type: MotionType
  content: string
  description?: string
  proposerId: string
  proposedAt: Date
  status: 'pending_second' | 'seconded' | 'debating' | 'voting' | 'passed' | 'rejected'
  secondedBy: string[]
  secondsRequired: number
  votes?: VoteRecord[]
}

type MotionType
  = | 'main' // 实质动议
    | 'subsidiary' // 程序动议
    | 'privileged' // 特权动议
    | 'incidental' // 附带动议
    | 'other';

interface VoteRecord {
  userId: string
  vote: 'for' | 'against' | 'abstain'
  votedAt: Date
}

interface Participant {
  id: string
  userId: string
  userName: string
  role: 'host' | 'participant' | 'observer'
  hasFloor: boolean
  joinedAt: Date
}
```

---

## 8. 交互细节

### 8.1 发言权流转

1. 与会者点击"抢发言权" → 加入队列末尾
2. 当前发言者点击"结束发言" → 发言权自动移交给队列下一人
3. 队列为空时，发言权释放
4. 主持可手动分配发言权（点击与会者 → "分配发言权"）

### 8.2 动议生命周期

```
[提出] → [待附议] → [已附议] → [辩论中]（可选）→ [投票中] → [通过/否决]
```

- **待附议：** 需达到附议人数阈值（默认 2 人）
- **辩论中：** 允许正反方发言（可选环节）
- **投票中：** 全员收到投票弹窗，设置投票时限（如 60 秒）
- **通过/否决：** 根据投票结果决定（多数通过、2/3 通过等）

### 8.3 记录模式

- 开启后：所有操作限制解除，允许自由补录
- 典型场景：事后补录会议纪要、自由讨论环节
- 视觉提示：顶部显示警告横幅，操作按钮边框变为虚线

---

## 9. 验收标准

### 9.1 功能验收

- [ ] 主持可开启/结束会议
- [ ] 与会者可抢发言权并进入队列
- [ ] 发言权自动流转（队列先进先出）
- [ ] 有发言权者可提出动议（弹窗选择类型）
- [ ] 动议附议流程正常（达到附议数后进入投票）
- [ ] 投票弹窗正确显示，投票结果正确统计
- [ ] 记录模式可正常开启/关闭，限制正确解除
- [ ] 操作按钮状态根据会议状态动态更新

### 9.2 体验验收

- [ ] 单页布局，无页面跳转
- [ ] 弹窗交互流畅，点击外部可关闭
- [ ] 状态提示清晰（当前可执行操作）
- [ ] 发言队列实时更新
- [ ] 动议卡片层次分明（议题 vs 动议）
- [ ] 记录模式有明确视觉提示

### 9.3 兼容性

- [ ] 响应式布局，支持 1440px 桌面和 1920px 宽屏
- [ ] 与视频会议软件并排使用时，界面不遮挡关键内容

---

## 10. 待确认问题

- [ ] 附议人数阈值是否可配置？（建议默认 2 人，可在会议设置中调整）
- [ ] 投票是否需要倒计时？时限可配置？
- [ ] 是否需要支持"紧急动议"（打断当前发言）？
- [ ] 观察员是否有任何可执行操作？
- [ ] 会议纪要导出功能是否在本原型范围内？

---

## 11. 下一步

**本规划文档已准备就绪，请审核以下关键设计决策：**

1. **单页布局**：顶部栏 + 左中右三栏 + 底部历史栏，是否符合预期？
2. **弹窗范围**：动议弹窗、投票弹窗、会议设置弹窗、与会者详情弹窗，是否足够？
3. **操作约束**：标准模式下严格执行罗伯特议事规则，记录模式解除限制，是否合理？
4. **优先级**：若时间有限，哪些交互可以简化（如发言队列拖拽、历史详情弹窗）？

**审核通过后，我将进入 Design 模式，生成完整的 HTML 原型（含弹窗交互）。**