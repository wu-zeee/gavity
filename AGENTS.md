# Gavity Agent Guide

在席 Gavity 是一个由可执行议事规则约束的人类—Agent 协作会议系统。

核心原则：

> Agent 可以代表人参与，但不能代表人越权。

## 开始任务前

按任务需要阅读以下事实来源：

- `docs/product.md`：产品定位、用户、角色和原则；
- `docs/mvp.md`：MVP 范围、非目标和完成标准；
- `docs/architecture.md`：目标架构、数据流和安全边界；
- `docs/domain-rules.md`：会议、动议、授权和升级规则；
- `docs/demo-script.md`：固定演示与端到端验收场景；
- `docs/development-plan.md`：开发顺序和阶段验收。

发生冲突时，优先遵循用户本轮明确要求，其次是上述文档，再其次是现有实现。不要默默替产品做新的重大决定；记录假设或请求确认。

## 技术栈与现有架构

- Bun；
- Nuxt v4、Vue、Nuxt UI；
- TypeScript、Zod；
- Drizzle ORM；
- Better Auth；
- Cloudflare Workers、D1；
- 目标实时层为 Cloudflare Durable Objects/WebSocket。

尊重现有架构，不大规模重写，不重复造轮子。复杂或共享业务逻辑使用 `utils` 和纯 TypeScript 模块，不创建承载大量业务状态的大型 composable。

## 目录职责

- `app/components/`：界面展示和轻量交互；
- `app/utils/`：客户端协调、状态适配和 API 调用；
- `server/api/`：认证后的 HTTP/实时入口；
- `server/services/`：应用服务、通知和 Agent 编排；
- `server/utils/db/`：Drizzle Schema、数据库和 migration；
- `shared/domain/`：客户端与服务端共用的确定性领域核心；
- `shared/contracts/`：API 请求和响应 Schema；
- `tests/`：领域、集成和端到端测试。

新增领域逻辑时优先写入 `shared/domain/`，不要继续扩大 `app/utils/meetings.ts` 的前端内存职责。

## 不可破坏的领域边界

1. 服务端是会议状态的唯一权威来源。
2. 客户端、AI 主持人和数字代表只能提交命令，不能直接写入状态。
3. 身份、授权、法定人数、状态转换、票数和表决门槛由确定性代码处理。
4. LLM 只负责自然语言理解、总结、解释和候选动作生成。
5. AI 主持人与个人数字代表必须是独立角色。
6. 数字代表的每次动作都必须引用有效授权。
7. 越权动作在真人确认前不得生效。
8. 真人确认必须限定会议、议题、动作、次数和有效期。
9. 重要动作写入版本化事件日志，可追溯且不可用摘要替代。
10. 数字代表不得声称自己是真人。
11. Photon iMessage 是 MVP 真人升级主链路；站内确认只作为开发、测试和故障回退。

## 数据和 API 规则

- 所有外部输入使用 Zod 或等价 Schema 校验；
- 服务端从认证会话推导当前用户，不信任客户端提交的 actor ID；
- 写入命令必须支持幂等；
- 同一会议的状态变更必须串行化；
- 表决和授权截止时间以服务端为准；
- 数据库 Schema 变更必须带 migration；
- 私人授权只返回完成当前功能所需的最小字段；
- 秘密或敏感数据不得进入客户端日志和模型上下文；
- 密钥只放在 `.dev.vars` 或部署平台 Secret，不提交到 Git。
- Photon 凭据、回调密钥和测试号码不得写入仓库、日志或测试夹具。

## 演示模式

- 可以保留固定演示数据和自动代理；
- 演示行为必须确定、可复现，禁止用随机投票决定结果；
- 演示动作仍需经过身份、授权、规则和事件日志；
- 演示模式必须与真实数据明确隔离；
- `docs/demo-script.md` 是演示行为的验收来源。
- 正式比赛演示必须完成真实 Photon iMessage 发送与回复；站内确认不能冒充该主链路。

## UI 约束

- 保留现有会议控制台的信息架构，除非任务明确要求重设计；
- 关键状态必须同时通过文本和颜色表达；
- 所有 Agent 行为明确标识角色和主人；
- 界面不得把“AI 建议”展示为“正式裁决”；
- 越权、等待真人确认和授权撤销必须显著可见；
- 优先保持键盘可用、颜色对比和清晰焦点状态。

## 本地命令

```bash
bun install
bun dev
bun run lint
bun test
bun run build
```

- 开发服务器通过 `https://gavity.localhost` 访问；
- 当前仓库尚未建立完整测试基础设施；阶段二开始必须补充测试；
- 如遇 ESLint 问题，先运行 `bun run lint --fix`，无法自动修复再编辑；
- 不通过删除测试、降低规则或静默忽略错误来让检查变绿。

## 实施流程

每项任务应明确：

- **Goal**：唯一目标；
- **Context**：相关文档、文件和现有行为；
- **Constraints**：架构、安全和非目标；
- **Done when**：用户可观察结果、测试和命令。

复杂任务先给出计划，再按可验证切片实现。修改时：

1. 先读相关文档和现有实现；
2. 保护用户已有改动，避免无关重构；
3. 先写或更新测试，再验证行为；
4. 运行相关测试、lint 和 build；
5. 检查完整 diff；
6. 报告已验证内容、已知限制和剩余风险。

## 完成定义

除非任务另有说明，代码任务只有在以下条件满足时才算完成：

- 请求的用户行为已实现；
- 非法和边界行为有测试；
- 相关测试通过；
- `bun run lint` 通过；
- `bun run build` 通过；
- 没有提交密钥、调试后门或随机演示结果；
- 文档与实际行为保持一致；
- 最终 diff 不包含无关修改。
